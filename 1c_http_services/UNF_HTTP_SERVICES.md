# Публикация HTTP-сервисов в 1С:УНФ

## 1. Создание HTTP-сервиса

1. Откройте конфигурацию в **Конфигураторе**.
2. В дереве метаданных найдите ветку **Общие → HTTP-сервисы**.
3. Нажмите **Добавить** → создайте HTTP-сервис с именем **api**.
4. В поле **Корневой URL** укажите **api** (или оставьте пустым, если хотите `/hs/api` — тогда укажите `api`).

---

## 2. URL-шаблоны и модули

Для каждого эндпоинта создайте URL-шаблон внутри HTTP-сервиса `api`.

### 2.1. GET `/hs/api/stock`

**URL-шаблон:** `stock`  
**Метод:** GET  

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| item | string | нет | Фильтр по номенклатуре (частичное совпадение) |
| organization | string | нет | Фильтр по складу (частичное совпадение) |
| min_quantity | int | нет | Минимальный остаток |
| nomenclature | string | нет | Синоним `item` |
| warehouse | string | нет | Синоним `organization` |

**Формат ответа:**
```json
[
  {
    "item": "Гвоздь 100мм",
    "organization": "ООО СтройМастер",
    "quantity": 1500
  }
]
```

**Код обработчика (вставить в модуль URL-шаблона):**

```1c
Функция getStock(Запрос) Экспорт

    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    Если ПустаяСтрока(Товар) Тогда
        Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "nomenclature");
    КонецЕсли;

    Организация = ПолучитьПараметрHTTPЗапроса(Запрос, "organization");
    Если ПустаяСтрока(Организация) Тогда
        Организация = ПолучитьПараметрHTTPЗапроса(Запрос, "warehouse");
    КонецЕсли;

    МинКоличество = ПолучитьПараметрHTTPЗапроса(Запрос, "min_quantity");
    МинКоличествоЧисло = ?(ЗначениеЗаполнено(МинКоличество), Число(МинКоличество), 0);

    Выборка = РегистрыНакопления.ЗапасыНаСкладах.Выбрать(, );

    Остатки = Новый Соответствие;

    Попытка
        Пока Выборка.Следующий() Цикл

            // Фильтр по номенклатуре (частичное совпадение)
            Если ЗначениеЗаполнено(Товар) Тогда
                Если СтрНайти(ВРег(Выборка.Номенклатура), ВРег(Товар)) = 0 Тогда
                    Продолжить;
                КонецЕсли;
            КонецЕсли;

            // Фильтр по складу (частичное совпадение)
            // Используем измерение "Склад" регистра ЗапасыНаСкладах
            Если ЗначениеЗаполнено(Организация) Тогда
                Попытка
                    Если СтрНайти(ВРег(Выборка.Склад), ВРег(Организация)) = 0 Тогда
                        Продолжить;
                    КонецЕсли;
                Исключение
                    // Если поля "Склад" нет, пробуем "Организация"
                    Если СтрНайти(ВРег(Выборка.Организация), ВРег(Организация)) = 0 Тогда
                        Продолжить;
                    КонецЕсли;
                КонецПопытки;
            КонецЕсли;

            // Фильтр по минимальному количеству
            ИмяТовара = Строка(Выборка.Номенклатура);
            ИмяОрганизации = Строка(Выборка.Организация);
            Ключ = ИмяТовара + "|" + ИмяОрганизации;
            ТекКоличество = Выборка.Количество;

            Если Остатки[Ключ] = Неопределено Тогда
                Остатки[Ключ] = Новый Структура("item, organization, quantity", ИмяТовара, ИмяОрганизации, ТекКоличество);
            Иначе
                Остатки[Ключ].quantity = Остатки[Ключ].quantity + ТекКоличество;
            КонецЕсли;
        КонецЦикла;

        Массив = Новый Массив;
        Для Каждого КлючЗначение Из Остатки Цикл
            Если КлючЗначение.Значение.quantity = 0 Тогда
                Продолжить;
            КонецЕсли;
            Если МинКоличествоЧисло > 0 И КлючЗначение.Значение.quantity < МинКоличествоЧисло Тогда
                Продолжить;
            КонецЕсли;
            Массив.Добавить(КлючЗначение.Значение);
        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

**Что изменилось в коде:**
- Фильтрация по частичному совпадению (`СтрНайти`) вместо точного (`<>`)
- Добавлен фильтр `min_quantity`
- Поддержка синонимов: `item`/`nomenclature`, `organization`/`warehouse`
- Обработка ошибок через `Попытка/Исключение`

---

### 2.2. GET `/hs/api/sales`

**URL-шаблон:** `sales`  
**Метод:** GET  

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| date_from | string (ISO) | нет | Начальная дата (YYYY-MM-DD) |
| date_to | string (ISO) | нет | Конечная дата (YYYY-MM-DD) |
| manager | string | нет | ФИО менеджера (частичное совпадение) |
| item | string | нет | Название товара (частичное совпадение) |

**Формат ответа:**
```json
[
  {
    "date": "2025-01-15",
    "item": "Гвоздь 100мм",
    "quantity": 200,
    "sum": 4000.0,
    "manager": "Иванов И.И."
  }
]
```

**Код обработчика:**

```1c
Функция ПродажиЗаПериод(Запрос) Экспорт

    ДатаНач = ПолучитьПараметрHTTPЗапроса(Запрос, "date_from");
    ДатаКон = ПолучитьПараметрHTTPЗапроса(Запрос, "date_to");
    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    Менеджер = ПолучитьПараметрHTTPЗапроса(Запрос, "manager");

    Попытка
        Выборка = РегистрыНакопления.Продажи.Выбрать(, , );

        Массив = Новый Массив;
        Пока Выборка.Следующий() Цикл

            // date_from: оставляем только записи >= начальной даты
            Если ЗначениеЗаполнено(ДатаНач) И Выборка.Период < Дата(СтрЗаменить(ДатаНач, "-", "")) Тогда
                Продолжить;
            КонецЕсли;

            // date_to: оставляем только записи <= конечной даты (+1 день, т.к. Период может содержать время)
            Если ЗначениеЗаполнено(ДатаКон) И Выборка.Период >= Дата(СтрЗаменить(ДатаКон, "-", "")) + 86400 Тогда
                Продолжить;
            КонецЕсли;

            // Фильтр по товару (частичное совпадение, регистронезависимо)
            Если ЗначениеЗаполнено(Товар) И СтрНайти(ВРег(Выборка.Номенклатура), ВРег(Товар)) = 0 Тогда
                Продолжить;
            КонецЕсли;

            // Фильтр по менеджеру (частичное совпадение)
            Если ЗначениеЗаполнено(Менеджер) И СтрНайти(ВРег(Выборка.Ответственный), ВРег(Менеджер)) = 0 Тогда
                Продолжить;
            КонецЕсли;

            Стр = Новый Структура;
            Стр.Вставить("date", Формат(Выборка.Период, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("item", Строка(Выборка.Номенклатура));
            Стр.Вставить("quantity", Выборка.Количество);
            Стр.Вставить("sum", Выборка.Сумма);
            Стр.Вставить("manager", Строка(Выборка.Ответственный));
            Стр.Вставить("client", Строка(Выборка.Контрагент));  // 👈 добавить покупателя
            Массив.Добавить(Стр);

        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

**Что исправлено:**
- `date_to` — было `<` (как `date_from`), стало `>` — теперь работает как верхняя граница
- `item` — точное совпадение `<>` → частичное `СтрНайти(ВРег(), ВРег())`
- Добавлен фильтр `manager`
- Структура ответа: `item` вместо `nomenclature` (как и в stock)

---

### 2.3. GET `/hs/api/sales/by_manager`

**URL-шаблон:** `sales/by_manager`  
**Метод:** GET  

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| date_from | string (ISO) | нет | Начальная дата (YYYY-MM-DD) |
| date_to | string (ISO) | нет | Конечная дата (YYYY-MM-DD) |
| manager | string | нет | ФИО менеджера |

**Формат ответа:**
```json
[
  {
    "manager": "Иванов И.И.",
    "total_sum": 45000.0,
    "total_quantity": 500
  }
]
```

```1c
Функция getSalesByManager(Запрос) Экспорт

    ДатаНачала = Запрос.Параметры["date_from"];
    ДатаОкончания = Запрос.Параметры["date_to"];
    Менеджер = Запрос.Параметры["manager"];

    ТекстЗапроса =
    "ВЫБРАТЬ
    |    ПродажиОбороты.Ответственный.Наименование КАК Менеджер,
    |    СУММА(ПродажиОбороты.СтоимостьОборот) КАК ОбщаяСумма,
    |    СУММА(ПродажиОбороты.КоличествоОборот) КАК ОбщееКоличество
    |ИЗ
    |    РегистрНакопления.Продажи.Обороты КАК ПродажиОбороты
    |ГДЕ
    |    (&ДатаНачала = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ПродажиОбороты.Период >= &ДатаНачала)
    |    И (&ДатаОкончания = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ПродажиОбороты.Период <= &ДатаОкончания)
    |    И (&Менеджер = "" ИЛИ ПродажиОбороты.Ответственный.Наименование ПОДОБНО &Менеджер)
    |СГРУППИРОВАТЬ ПО
    |    ПродажиОбороты.Ответственный.Наименование";

    ЗапросОбъект = Новый Запрос(ТекстЗапроса);
    ЗапросОбъект.УстановитьПараметр("ДатаНачала", ?(ЗначениеЗаполнено(ДатаНачала), Дата(ДатаНачала), '00010101'));
    ЗапросОбъект.УстановитьПараметр("ДатаОкончания", ?(ЗначениеЗаполнено(ДатаОкончания), Дата(ДатаОкончания), '00010101'));
    ЗапросОбъект.УстановитьПараметр("Менеджер", ?(ЗначениеЗаполнено(Менеджер), "%" + Менеджер + "%", ""));

    Результат = ЗапросОбъект.Выполнить();
    Выборка = Результат.Выбрать();

    МассивРезультата = Новый Массив;

    Пока Выборка.Следующий() Цикл
        Строка = Новый Структура;
        Строка.Вставить("manager", Выборка.Менеджер);
        Строка.Вставить("total_sum", Выборка.ОбщаяСумма);
        Строка.Вставить("total_quantity", Выборка.ОбщееКоличество);
        МассивРезультата.Добавить(Строка);
    КонецЦикла;

    Возврат ПреобразоватьВJSON(МассивРезультата);

КонецФункции
```

---

### 2.4. GET `/hs/api/purchases`

**URL-шаблон:** `purchases`  
**Метод:** GET  

Закупки товаров/услуг у поставщиков (поступление).

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| date_from | string (ISO) | нет | Начальная дата (YYYY-MM-DD) |
| date_to | string (ISO) | нет | Конечная дата (YYYY-MM-DD) |
| item | string | нет | Название товара/услуги (частичное совпадение) |
| supplier | string | нет | Поставщик (частичное совпадение) |

**Формат ответа:**
```json
[
  {
    "date": "2025-01-15",
    "item": "Гвоздь 100мм",
    "quantity": 500,
    "sum": 6250.0,
    "supplier": "ООО Метизы"
  }
]
```

**Код обработчика:**

```1c
Функция ЗакупкиЗаПериод(Запрос) Экспорт

    ДатаНач = ПолучитьПараметрHTTPЗапроса(Запрос, "date_from");
    ДатаКон = ПолучитьПараметрHTTPЗапроса(Запрос, "date_to");
    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    Поставщик = ПолучитьПараметрHTTPЗапроса(Запрос, "supplier");

    Попытка
        // Используем регистр закупок (поступления товаров)
        // В УНФ это может быть РегистрНакопления.Закупки или РегистрНакопления.ПоступлениеТоваров
        Выборка = РегистрыНакопления.Закупки.Выбрать(, , );

        Массив = Новый Массив;
        Пока Выборка.Следующий() Цикл

            Если ЗначениеЗаполнено(ДатаНач) И Выборка.Период < Дата(СтрЗаменить(ДатаНач, "-", "")) Тогда
                Продолжить;
            КонецЕсли;

            Если ЗначениеЗаполнено(ДатаКон) И Выборка.Период >= Дата(СтрЗаменить(ДатаКон, "-", "")) + 86400 Тогда
                Продолжить;
            КонецЕсли;

            Если ЗначениеЗаполнено(Товар) И СтрНайти(ВРег(Выборка.Номенклатура), ВРег(Товар)) = 0 Тогда
                Продолжить;
            КонецЕсли;

            Если ЗначениеЗаполнено(Поставщик) И СтрНайти(ВРег(Выборка.Контрагент), ВРег(Поставщик)) = 0 Тогда
                Продолжить;
            КонецЕсли;

            Стр = Новый Структура;
            Стр.Вставить("date", Формат(Выборка.Период, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("item", Строка(Выборка.Номенклатура));
            Стр.Вставить("quantity", Выборка.Количество);
            Стр.Вставить("sum", Выборка.Сумма);
            Стр.Вставить("supplier", Строка(Выборка.Контрагент));
            Массив.Добавить(Стр);

        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

> ⚠️ Если в вашей конфигурации регистр называется иначе (например, `ПоступлениеТоваров`), замените `РегистрыНакопления.Закупки` на правильное имя.

---

### 2.5. GET `/hs/api/receivables`

**URL-шаблон:** `receivables`  
**Метод:** GET  

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| min_amount | float | нет | Минимальная сумма долга |
| date_from | string (ISO) | нет | Начальная дата |

**Формат ответа:**
```json
[
  {
    "client": "ООО СтройМастер",
    "amount": 250000.0,
    "overdue_days": 45
  }
]
```

```1c
Функция getReceivables(Запрос) Экспорт

    МинСумма = Запрос.Параметры["min_amount"];
    ДатаНачала = Запрос.Параметры["date_from"];

    // Для УНФ используем регистр Взаиморасчёты
    // Если в вашей конфигурации его нет, используйте ВзаиморасчетыСКлиентами
    ТекстЗапроса =
    "ВЫБРАТЬ
    |    ВзаиморасчетыОстатки.Контрагент.Наименование КАК Клиент,
    |    ВзаиморасчетыОстатки.СуммаОстаток КАК Сумма,
    |    0 КАК ДнейПросрочки
    |ИЗ
    |    РегистрНакопления.Взаиморасчеты.Остатки КАК ВзаиморасчетыОстатки
    |ГДЕ
    |    ВзаиморасчетыОстатки.СуммаОстаток < 0
    |    И (&МинСумма = 0 ИЛИ (-ВзаиморасчетыОстатки.СуммаОстаток) >= &МинСумма)";

    ЗапросОбъект = Новый Запрос(ТекстЗапроса);
    ЗапросОбъект.УстановитьПараметр("МинСумма", ?(ЗначениеЗаполнено(МинСумма), Число(МинСумма), 0));

    Результат = ЗапросОбъект.Выполнить();
    Выборка = Результат.Выбрать();

    МассивРезультата = Новый Массив;
    ТекущаяДата = ТекущаяДата();

    Пока Выборка.Следующий() Цикл
        Строка = Новый Структура;
        Строка.Вставить("client", Выборка.Клиент);
        Строка.Вставить("amount", -Выборка.Сумма);

        // Рассчитываем дни просрочки (упрощённо)
        // В реальности нужно запрашивать ЗадолженностьПоСрокам
        Строка.Вставить("overdue_days", 0);
        МассивРезультата.Добавить(Строка);
    КонецЦикла;

    Возврат ПреобразоватьВJSON(МассивРезультата);

КонецФункции
```

---

### 2.5. GET `/hs/api/nomenclature/search`

**URL-шаблон:** `nomenclature/search`  
**Метод:** GET  

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| q | string | да | Поисковый запрос |
| limit | int | нет | Лимит результатов (по умолчанию 10) |

**Формат ответа:**
```json
[
  {
    "ref": "00000001",
    "name": "Гвоздь 100мм",
    "unit": "шт",
    "item_type": "Товар"
  }
]
```

```1c
Функция searchNomenclature(Запрос) Экспорт

    Поиск = Запрос.Параметры["q"];
    Лимит = Запрос.Параметры["limit"];

    // ⚠️ Проблема: IIS не передаёт UTF-8 параметры в 1С корректно.
    // Если поиск по кириллице не работает, используйте decodeURI:
    // Поиск = РаскодироватьСтроку(Запрос.Параметры["q"], СпособКодировкиСтроки.URLКодирование);

    Если Не ЗначениеЗаполнено(Лимит) Тогда
        Лимит = "10";
    КонецЕсли;

    ТекстЗапроса =
    "ВЫБРАТЬ ПЕРВЫЕ &Лимит
    |    Номенклатура.Ссылка КАК Ссылка,
    |    Номенклатура.Наименование КАК Наименование,
    |    Номенклатура.ЕдиницаИзмерения.Наименование КАК Единица,
    |    Номенклатура.ВидНоменклатуры.Наименование КАК Тип
    |ИЗ
    |    Справочник.Номенклатура КАК Номенклатура
    |ГДЕ
    |    Номенклатура.Наименование ПОДОБНО &Поиск
    |    И Номенклатура.ПометкаУдаления = Ложь
    |УПОРЯДОЧИТЬ ПО
    |    Номенклатура.Наименование";

    ЗапросОбъект = Новый Запрос(ТекстЗапроса);
    ЗапросОбъект.УстановитьПараметр("Поиск", "%" + Поиск + "%");
    ЗапросОбъект.УстановитьПараметр("Лимит", Число(Лимит));

    Результат = ЗапросОбъект.Выполнить();
    Выборка = Результат.Выбрать();

    МассивРезультата = Новый Массив;

    Пока Выборка.Следующий() Цикл
        Строка = Новый Структура;
        Строка.Вставить("ref", Строка(Выборка.Ссылка.УникальныйИдентификатор()));
        Строка.Вставить("name", Выборка.Наименование);
        Строка.Вставить("unit", Выборка.Единица);
        Строка.Вставить("item_type", Строка(Выборка.Тип));  // 👈 тип: Товар / Услуга / Работа
        МассивРезультата.Добавить(Строка);
    КонецЦикла;

    Возврат ПреобразоватьВJSON(МассивРезультата);

КонецФункции
```

---

## 3. Общая функция преобразования в JSON

Добавьте эту функцию в **общий модуль** (например, `HTTPСервисыВызовСервера`) или в каждый модуль URL-шаблона:

```1c
Функция ПреобразоватьВJSON(Данные) Экспорт

    ЗаписьJSON = Новый ЗаписьJSON;
    Результат = Новый Массив;

    // Определяем тип данных
    Если ТипЗнч(Данные) = Тип("Массив") Тогда
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, Данные);
        Возврат ЗаписьJSON.Закрыть();
    ИначеЕсли ТипЗнч(Данные) = Тип("Структура") Или ТипЗнч(Данные) = Тип("Соответствие") Тогда
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, Данные);
        Возврат ЗаписьJSON.Закрыть();
    Иначе
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, Данные);
        Возврат ЗаписьJSON.Закрыть();
    КонецЕсли;

КонецФункции
```

---

## 4. Настройка публикации на веб-сервере

### Apache
```apache
<Location /unf-kav>
    AuthType Basic
    AuthName "1C:Enterprise"
    AuthUserFile /etc/apache2/.htpasswd
    Require valid-user
</Location>
```

### IIS
1. Откройте IIS Manager.
2. Выберите сайт → **Authentication** → включите **Basic Authentication**.
3. Создайте пользователя с правами только на чтение.

### nginx
```nginx
location /unf-kav {
    auth_basic "1C:Enterprise";
    auth_basic_user_file /etc/nginx/.htpasswd;
    proxy_pass http://127.0.0.1:8080/unf-kav;
}
```

### 2.7. GET `/hs/api/price-history`

**URL-шаблон:** `price-history`  
**Метод:** GET  

История изменения цен на товар.

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| item | string | да | Название товара |
| date_from | string (ISO) | нет | Начальная дата |
| date_to | string (ISO) | нет | Конечная дата |
| limit | int | нет | Лимит записей (по умолчанию 20) |

**Формат ответа:**
```json
[
  {"date": "2026-01-15", "price": 100.0, "change_percent": 0},
  {"date": "2026-03-01", "price": 110.0, "change_percent": 10},
  {"date": "2026-05-10", "price": 120.0, "change_percent": 9.1}
]
```

**Код обработчика:**

```1c
Функция ИсторияЦен(Запрос) Экспорт

    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    ДатаНач = ПолучитьПараметрHTTPЗапроса(Запрос, "date_from");
    ДатаКон = ПолучитьПараметрHTTPЗапроса(Запрос, "date_to");
    Лимит = Число(ПолучитьПараметрHTTPЗапроса(Запрос, "limit")) ИЛИ 20;

    Если Не ЗначениеЗаполнено(Товар) Тогда
        Ответ = Новый HTTPСервисОтвет(400);
        Ответ.УстановитьТелоИзСтроки("{""error"": ""Не указан товар""}");
        Возврат Ответ;
    КонецЕсли;

    Попытка
        ЗапросОбъект = Новый Запрос(
        "ВЫБРАТЬ ПЕРВЫЕ &Лимит
        |    ЦеныНоменклатуры.Период КАК Дата,
        |    ЦеныНоменклатуры.Цена КАК Цена
        |ИЗ
        |    РегистрСведений.ЦеныНоменклатуры КАК ЦеныНоменклатуры
        |ГДЕ
        |    ЦеныНоменклатуры.Номенклатура.Наименование ПОДОБНО &Товар
        |    И (&ДатаНач = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ЦеныНоменклатуры.Период >= &ДатаНач)
        |    И (&ДатаКон = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ЦеныНоменклатуры.Период <= &ДатаКон)
        |УПОРЯДОЧИТЬ ПО
        |    ЦеныНоменклатуры.Период");

        ЗапросОбъект.УстановитьПараметр("Товар", "%" + Товар + "%");
        ЗапросОбъект.УстановитьПараметр("Лимит", Лимит);
        ЗапросОбъект.УстановитьПараметр("ДатаНач", ?(ЗначениеЗаполнено(ДатаНач), Дата(ДатаНач), '00010101'));
        ЗапросОбъект.УстановитьПараметр("ДатаКон", ?(ЗначениеЗаполнено(ДатаКон), Дата(ДатаКон), '00010101'));

        Результат = ЗапросОбъект.Выполнить();
        Выборка = Результат.Выбрать();

        Массив = Новый Массив;
        ПредыдущаяЦена = 0;
        Пока Выборка.Следующий() Цикл
            Стр = Новый Структура;
            Стр.Вставить("date", Формат(Выборка.Дата, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("price", Выборка.Цена);
            Стр.Вставить("change_percent", ?(ПредыдущаяЦена > 0, (Выборка.Цена - ПредыдущаяЦена) / ПредыдущаяЦена * 100, 0));
            Массив.Добавить(Стр);
            ПредыдущаяЦена = Выборка.Цена;
        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

> ⚠️ Регистр может называться `ЦеныНоменклатуры` или `ЦеныТоваров` — проверьте в вашей конфигурации.

---

### 2.8. GET `/hs/api/purchase-orders`

**URL-шаблон:** `purchase-orders`  
**Метод:** GET  

Открытые заказы поставщикам (ожидаемые поставки).

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| item | string | нет | Название товара (частичное совпадение) |
| supplier | string | нет | Поставщик |
| status | string | нет | Статус: open/overdue/completed (по умолчанию open) |

**Формат ответа:**
```json
[
  {
    "date": "2026-06-15",
    "item": "Гвоздь 100мм",
    "quantity": 500,
    "expected_date": "2026-06-25",
    "supplier": "ООО Метизы",
    "status": "open",
    "days_overdue": 0
  }
]
```

**Код обработчика:**

```1c
Функция ЗаказыПоставщикам(Запрос) Экспорт

    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    Поставщик = ПолучитьПараметрHTTPЗапроса(Запрос, "supplier");
    Статус = ПолучитьПараметрHTTPЗапроса(Запрос, "status");

    Попытка
        ЗапросОбъект = Новый Запрос(
        "ВЫБРАТЬ
        |    Заказы.Дата КАК Дата,
        |    Заказы.Номенклатура.Наименование КАК Товар,
        |    Заказы.Количество КАК Количество,
        |    Заказы.ДатаОжидания КАК ОжидаемаяДата,
        |    Заказы.Контрагент.Наименование КАК Поставщик,
        |    Заказы.Статус КАК Статус
        |ИЗ
        |    Документ.ЗаказПоставщику.Состав КАК Заказы
        |ГДЕ
        |    (&Товар = "" ИЛИ Заказы.Номенклатура.Наименование ПОДОБНО &Товар)
        |    И (&Поставщик = "" ИЛИ Заказы.Контрагент.Наименование ПОДОБНО &Поставщик)");

        ЗапросОбъект.УстановитьПараметр("Товар", "%" + Товар + "%");
        ЗапросОбъект.УстановитьПараметр("Поставщик", "%" + Поставщик + "%");

        Результат = ЗапросОбъект.Выполнить();
        Выборка = Результат.Выбрать();

        Массив = Новый Массив;
        ТекущаяДата = ТекущаяДата();
        Пока Выборка.Следующий() Цикл
            Стр = Новый Структура;
            Стр.Вставить("date", Формат(Выборка.Дата, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("item", Выборка.Товар);
            Стр.Вставить("quantity", Выборка.Количество);
            Стр.Вставить("expected_date", Формат(Выборка.ОжидаемаяДата, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("supplier", Выборка.Поставщик);
            Стр.Вставить("status", Выборка.Статус);
            Дней = ?(Выборка.ОжидаемаяДата < ТекущаяДата, (ТекущаяДата - Выборка.ОжидаемаяДата) / 86400, 0);
            Стр.Вставить("days_overdue", Цел(Дней));
            Массив.Добавить(Стр);
        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

---

### 2.9. GET `/hs/api/item-movement`

**URL-шаблон:** `item-movement`  
**Метод:** GET  

Движение товара: приход (поступление) и расход (продажи) по дням.

**Параметры запроса (query):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| item | string | да | Название товара |
| date_from | string (ISO) | нет | Начальная дата |
| date_to | string (ISO) | нет | Конечная дата |

**Формат ответа:**
```json
[
  {"date": "2026-01-15", "incoming": 500, "outgoing": 0, "type": "purchase"},
  {"date": "2026-01-16", "incoming": 0, "outgoing": 100, "type": "sale"},
  {"date": "2026-01-17", "incoming": 0, "outgoing": 50, "type": "sale"}
]
```

```1c
Функция ДвижениеТовара(Запрос) Экспорт

    Товар = ПолучитьПараметрHTTPЗапроса(Запрос, "item");
    ДатаНач = ПолучитьПараметрHTTPЗапроса(Запрос, "date_from");
    ДатаКон = ПолучитьПараметрHTTPЗапроса(Запрос, "date_to");

    Если Не ЗначениеЗаполнено(Товар) Тогда
        Ответ = Новый HTTPСервисОтвет(400);
        Ответ.УстановитьТелоИзСтроки("{""error"": ""Не указан товар""}");
        Возврат Ответ;
    КонецЕсли;

    Попытка
        // Поступления (закупки)
        ТекстЗапроса =
        "ВЫБРАТЬ
        |    Поступление.Период КАК Дата,
        |    Поступление.КоличествоОборот КАК Приход
        |ИЗ
        |    РегистрНакопления.Закупки.Обороты КАК Поступление
        |ГДЕ
        |    Поступление.Номенклатура.Наименование ПОДОБНО &Товар
        |    И (&ДатаНач = ДАТАВРЕМЯ(1, 1, 1) ИЛИ Поступление.Период >= &ДатаНач)
        |    И (&ДатаКон = ДАТАВРЕМЯ(1, 1, 1) ИЛИ Поступление.Период <= &ДатаКон)
        |СГРУППИРОВАТЬ ПО
        |    Поступление.Период
        |
        |ОБЪЕДИНИТЬ ВСЕ
        |
        |ВЫБРАТЬ
        |    Продажи.Период КАК Дата,
        |    -Продажи.КоличествоОборот КАК Расход
        |ИЗ
        |    РегистрНакопления.Продажи.Обороты КАК Продажи
        |ГДЕ
        |    Продажи.Номенклатура.Наименование ПОДОБНО &Товар
        |    И (&ДатаНач = ДАТАВРЕМЯ(1, 1, 1) ИЛИ Продажи.Период >= &ДатаНач)
        |    И (&ДатаКон = ДАТАВРЕМЯ(1, 1, 1) ИЛИ Продажи.Период <= &ДатаКон)
        |УПОРЯДОЧИТЬ ПО
        |    Дата";

        ЗапросОбъект = Новый Запрос(ТекстЗапроса);
        ЗапросОбъект.УстановитьПараметр("Товар", "%" + Товар + "%");
        ЗапросОбъект.УстановитьПараметр("ДатаНач", ?(ЗначениеЗаполнено(ДатаНач), Дата(ДатаНач), '00010101'));
        ЗапросОбъект.УстановитьПараметр("ДатаКон", ?(ЗначениеЗаполнено(ДатаКон), Дата(ДатаКон), '00010101'));

        Результат = ЗапросОбъект.Выполнить();
        Выборка = Результат.Выбрать();

        Массив = Новый Массив;
        Пока Выборка.Следующий() Цикл
            Стр = Новый Структура;
            incoming = ?(Выборка.КоличествоОборот > 0, Выборка.КоличествоОборот, 0);
            outgoing = ?(Выборка.КоличествоОборот < 0, -Выборка.КоличествоОборот, 0);
            Стр.Вставить("date", Формат(Выборка.Дата, "ДФ=yyyy-MM-dd"));
            Стр.Вставить("incoming", incoming);
            Стр.Вставить("outgoing", outgoing);
            Стр.Вставить("type", ?(incoming > 0, "purchase", "sale"));
            Массив.Добавить(Стр);
        КонецЦикла;

        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        Ответ.УстановитьТелоИзСтроки(ЗначениеВСтрокуВнутр2(Массив));
        Возврат Ответ;
    Исключение
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.УстановитьТелоИзСтроки("{""error"": """ + ОписаниеОшибки() + """}");
        Возврат Ответ;
    КонецПопытки;

КонецФункции
```

> ⚠️ В запросе используется `РегистрНакопления.Закупки` для поступлений и `РегистрНакопления.Продажи` для продаж. Замените имена регистров на актуальные.

---

## 5. Проверка

После публикации проверьте через curl:

```bash
# Остатки
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/stock"

# Остатки с фильтром
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/stock?warehouse=Основной"

# Продажи
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/sales?date_from=2026-01-01&date_to=2026-06-17"

# Продажи по менеджерам
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/sales/by_manager"

# Задолженность
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/receivables?min_amount=100000"

# Поиск номенклатуры
curl -u "username:password" "https://01.yes-it.ru/unf-kav/hs/api/nomenclature/search?q=гвоздь"
```

---

## 6. Известные проблемы и их решение

### 6.1. Поиск по кириллице не работает (nomenclature/search)

**Симптом:** GET `/hs/api/nomenclature/search?q=гвоздь` возвращает `[]`, хотя пустой поиск (`q=`) или поиск по цифрам работает.

**Причина:** IIS передаёт URL-параметры в 1С без корректной декодировки UTF-8. 1С получает параметр, но не может найти совпадение в базе.

**Решение (вариант А):** Добавить ручное декодирование в начале функции:

```1c
Функция searchNomenclature(Запрос) Экспорт

    Поиск = Запрос.Параметры["q"];

    // Ручное декодирование URL, т.к. IIS может не декодировать UTF-8
    Попытка
        Поиск = РаскодироватьСтроку(Поиск, СпособКодировкиСтроки.URLКодирование);
    Исключение
        // Если не получилось — оставляем как есть
    КонецПопытки;

    // ... остальной код
```

**Решение (вариант Б):** Создать отдельный POST-эндпоинт для поиска, где параметр `q` передаётся в теле запроса (JSON) — это гарантирует корректную передачу UTF-8.

### 6.2. Фильтрация stock/sales не работает

**Симптом:** Параметры `?warehouse=...&nomenclature=...` игнорируются, возвращаются все записи.

**Причина:** В `WHERE` отсутствуют скобки вокруг `ИЛИ`, из-за чего приоритет операторов неправильный:
```
// НЕПРАВИЛЬНО — AND имеет приоритет над OR
ГДЕ Количество > 0 И Склад = "" ИЛИ Склад ПОДОБНО &Склад

// ПРАВИЛЬНО
ГДЕ Количество > 0 И (Склад = "" ИЛИ Склад ПОДОБНО &Склад)
```

**Решение:** Использовать скобки как в исправленных примерах выше. После исправления переопубликуйте HTTP-сервис и проверьте:

```bash
curl -u "user:pass" "https://01.yes-it.ru/unf-kav/hs/api/stock?nomenclature=Гофрокороб"
```

### 6.3. В ответе stock/sales нет поля warehouse

**Симптом:** Ответ содержит `item`, `quantity`, но нет `warehouse`.

**Причина:** В 1С-коде используется JSON-ключ `item` вместо `nomenclature`, и пропущены поля `warehouse`/`unit`.

**Решение:** Проверьте, что в коде используются следующие ключи JSON (как в исправленных примерах):

| Поле в 1С | Ключ JSON | Описание |
|-----------|-----------|----------|
| Номенклатура | `nomenclature` | Название товара |
| Склад | `warehouse` | Название склада |
| Количество | `quantity` | Количество |
| Сумма | `sum` | Сумма продажи |
| Единица | `unit` | Единица измерения |

### 6.4. /receivables возвращает пустой массив

**Симптом:** GET `/hs/api/receivables` возвращает `[]`.

**Причина:**
- В базе действительно нет задолженностей (долгов клиентов)
- Или регистр `Взаиморасчеты` пуст/называется иначе

**Диагностика:** Проверьте в 1С, есть ли записи в регистре `Взаиморасчеты` с отрицательным остатком. Если регистр называется иначе (например, `ВзаиморасчетыСКлиентами`), замените имя в запросе.

## 7. Примечания по УНФ

- **Регистр "ПартииТоваров"** — содержит остатки по складам
- **Регистр "Продажи"** — содержит обороты по продажам
- **Регистр "Взаиморасчеты"** — содержит взаиморасчеты с контрагентами (отрицательный остаток = долг клиента)
- Если в вашей конфигурации имена регистров отличаются, замените их в запросах

## 8. Альтернатива: упрощённый вариант через OData

Если нет возможности публиковать HTTP-сервисы, можно временно использовать **OData**. Пример OData-запроса к справочнику номенклатуры:

```bash
curl -u "Андрей Кузнецов:250879" \
  "https://01.yes-it.ru/unf-kav/odata/standard.odata/Catalog_%D0%9D%D0%BE%D0%BC%D0%B5%D0%BD%D0%BA%D0%BB%D0%B0%D1%82%D1%83%D1%80%D0%B0?\$top=5&\$select=Ref,Description"
```

Но OData требует URL-кодирования русских имён и сложнее в парсинге. Рекомендуется публикация HTTP-сервисов.
