# 1С HTTP-сервисы: полная документация

## Batch-эндпоинт

### POST /hs/api/v1/batch

Выполняет несколько запросов к 1С в одном HTTP-вызове.

**Функция:** `ОбработатьBatch(Запрос)` — экспортная, размещается в модуле HTTP-сервиса.

**Вход** (JSON body):
```json
{
  "requests": [
    {"id": "stock", "method": "GET", "path": "/stock", "params": {"limit": "3"}},
    {"id": "sales", "method": "GET", "path": "/sales", "params": {"date_from": "2026-01-01", "date_to": "2026-06-24"}}
  ]
}
```

**Выход** (JSON):
```json
{
  "results": [
    {
      "id": "stock",
      "status": 200,
      "data": [{"item": "...", "organization": "...", "quantity": 100}],
      "execution_time_ms": 324
    },
    {
      "id": "sales",
      "status": 200,
      "data": [{"date": "2026-06-23", "item": "...", "quantity": 10, "sum": 1250, "manager": "...", "client": "..."}],
      "execution_time_ms": 93
    }
  ],
  "total_execution_time_ms": 417
}
```

**Файл:** `batch_endpoint.bsl`

---

## Фаза 4: Metadata + Execute Query

### GET /hs/metadata/config

Паспорт базы 1С (имя, конфигурация, версия, платформа).

**Файл:** `metadata_api.bsl`

**Функция:** `ConfigGET(Запрос)` — экспортная

**Пример:**
```bash
curl https://01.yes-it.ru/unf-kav/hs/metadata/config
```

### GET /hs/metadata/describe

Список объектов метаданных (справочники, документы, регистры).

**Параметры:** `type` (Catalog/Document/AccumulationRegister), `search`

**Функция:** `DescribeGET(Запрос)` — экспортная

### GET /hs/metadata/structure?name=Номенклатура

Структура объекта метаданных (поля, типы, синонимы).

**Функция:** `StructureGET(Запрос)` — экспортная

### POST /hs/query/execute

Выполнение произвольного запроса (только SELECT).

**Вход:** `{"query": "ВЫБРАТЬ ...", "params": {...}}`
**Выход:** `{"columns": [...], "rows": [[...], ...], "count": N}`

**Файл:** `execute_query.bsl`

**Функция:** `ExecutePOST(Запрос)` — экспортная

**Ограничения:**
- Только SELECT (ВЫБРАТЬ)
- Read-only guard: запрещены ИЗМЕНИТЬ/ДОБАВИТЬ/УДАЛИТЬ/ПРОВЕСТИ/ЗАПИСАТЬ

---

## Новые эндпоинты

### GET /hs/api/v1/documents/sales

Список документов реализации с пагинацией и фильтрацией.

**Функция:** `ПолучитьСписокРеализаций(Запрос)` — экспортная

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|---|---|---|---|
| `date_from` | date | ✅ | Дата начала (YYYY-MM-DD) |
| `date_to` | date | ✅ | Дата окончания (YYYY-MM-DD) |
| `counterparty` | string | ❌ | Фильтр по контрагенту (подстрока) |
| `sum_min` | float | ❌ | Минимальная сумма |
| `sum_max` | float | ❌ | Максимальная сумма |
| `posted_only` | bool | ❌ | Только проведённые (по умолч. true) |
| `sort_by` | string | ❌ | date / sum / number |
| `sort_order` | string | ❌ | asc / desc |
| `page` | int | ❌ | Номер страницы (по умолч. 1) |
| `page_size` | int | ❌ | Размер (по умолч. 50, макс 500) |

**Документ:** `Документ.РеализацияТоваровУслуг`

**Выход:**
```json
{
  "documents": [{"id": "uuid", "number": "ТС-000123", "date": "2026-01-05T10:30:00", "counterparty": {"name": "...", "id": "...", "inn": "..."}, "sum": 150000, "currency": "RUB", "posted": true, "deleted": false, "comment": "", "items_count": 5}],
  "pagination": {"page": 1, "page_size": 50, "total_count": 1234, "total_pages": 25}
}
```

## Отдельные эндпоинты (существующие)

### GET /hs/api/stock

Остатки товаров на складах.

**Функция:** `ОстаткиТовара(Запрос)`

**Параметры:** `item`, `nomenclature`, `organization`, `warehouse`, `min_quantity`

**Регистр:** `РегистрНакопления.ЗапасыНаСкладах.Остатки`

**Поля:** `Номенклатура.Наименование`, `Организация.Наименование`, `КоличествоОстаток`

**Выход:** `[{"item": "...", "organization": "...", "quantity": 100}]`

---

### GET /hs/api/sales

Продажи за период.

**Функция:** `ПродажиЗаПериод(Запрос)`

**Параметры:** `date_from`, `date_to`, `item`, `manager`, `limit`

**Регистр:** `РегистрНакопления.Продажи`

**Поля:** `Период`, `Номенклатура`, `Количество`, `Сумма`, `Ответственный`, `Контрагент`

**Выход:** `[{"date": "2026-06-23", "item": "...", "quantity": 10, "sum": 1250, "manager": "...", "client": "..."}]`

---

### GET /hs/api/sales/by_manager

Продажи по менеджерам.

**Функция:** `ПродажиПоМенеджерам(Запрос)`

**Параметры:** `date_from`, `date_to`, `manager`, `limit`

**Регистр:** `РегистрНакопления.Продажи`

**Выход:** `[{"manager": "...", "total_sum": 100000, "total_quantity": 500}]`

---

### GET /hs/api/receivables

Задолженность клиентов.

**Функция:** `ЗадолженностьКлиентов(Запрос)`

**Параметры:** `min_amount`

**Регистр:** `РегистрНакопления.РасчетыСПокупателями`

**Выход:** `[{"client": "...", "amount": 125000, "overdue_days": 0}]`

---

### GET /hs/api/purchases

Закупки у поставщиков.

**Функция:** `ЗакупкиЗаПериод(Запрос)`

**Параметры:** `date_from`, `date_to`, `item`, `supplier`

**Регистр:** `РегистрНакопления.Закупки`

**Выход:** `[{"date": "2026-06-23", "item": "...", "quantity": 10, "sum": 1250, "supplier": "..."}]`

---

### GET /hs/api/nomenclature/search

Поиск номенклатуры.

**Функция:** `ПоискНоменклатуры(Запрос)`

**Параметры:** `q`, `limit`

**Справочник:** `Справочник.Номенклатура`

**Выход:** `[{"ref": "UUID", "name": "...", "unit": "...", "item_type": "..."}]`

---

### GET /hs/api/pricehistory

История цен.

**Функция:** `ИсторияЦен(Запрос)`

**Параметры:** `item`, `date_from`, `date_to`, `limit`

**Регистр:** `РегистрСведений.ЦеныНоменклатуры`

**Выход:** `[{"date": "2026-06-23", "price": 150, "change_percent": 5.0}]`

---

### GET /hs/api/purchaseorders

Заказы поставщикам.

**Функция:** `ЗаказыПоставщикам(Запрос)`

**Параметры:** `item`, `supplier`, `status`

**Документ:** `Документ.ЗаказПоставщику.Запасы`

**Выход:** `[{"date": "2026-06-23", "item": "...", "quantity": 100, "expected_date": "2026-07-15", "supplier": "...", "days_overdue": 0}]`

---

### GET /hs/api/itemmovement

Движение товара.

**Функция:** `ДвижениеТовара(Запрос)`

**Параметры:** `item`, `date_from`, `date_to`

**Регистр:** `РегистрНакопления.Продажи`

**Выход:** `[{"date": "2026-06-23", "incoming": 0, "outgoing": 10}]`

---

## Вспомогательные функции модуля

### `ПолучитьПараметрHTTPЗапроса(Запрос, ИмяПараметра)`

Извлекает параметр из URL или тела запроса.

### `ЗначениеВСтрокуВнутр2(Значение)`

Сериализует значение 1С в JSON-строку через `ЗаписатьJSON`.

```bsl
Функция ЗначениеВСтрокуВнутр2(Значение)
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, Значение);
    Возврат ЗаписьJSON.Закрыть();
КонецФункции
```

### `СериализоватьJSON(Значение)`

Ручная сериализация в JSON (для старых платформ без `ЗаписатьJSON`).

### `ПреобразоватьВJSON(Данные)`

Обёртка над `ЗаписатьJSON` с поддержкой Массив, Структура, Соответствие.

### `СоздатьОтвет(КодСостояния, Данные)`

Формирует HTTP-ответ с JSON-телом.

```bsl
Функция СоздатьОтвет(КодСостояния, Данные)
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, Данные);
    СтрJSON = ЗаписьJSON.Закрыть();
    Ответ = Новый HTTPСервисОтвет(КодСостояния);
    Ответ.Заголовки.Вставить("Content-Type", "application/json;charset=utf-8");
    Ответ.УстановитьТелоИзСтроки(СтрJSON);
    Возврат Ответ;
КонецФункции
```

### `ПрочитатьТелоЗапроса(Запрос)`

Читает и парсит JSON-тело HTTP-запроса.

```bsl
Функция ПрочитатьТелоЗапроса(Запрос)
    ТелоТекст = Запрос.ПолучитьТелоКакСтроку();
    Если ПустаяСтрока(ТелоТекст) Тогда
        Возврат Неопределено;
    КонецЕсли;
    Чтение = Новый ЧтениеJSON;
    Чтение.УстановитьСтроку(ТелоТекст);
    Результат = ПрочитатьJSON(Чтение);
    Чтение.Закрыть();
    Возврат Результат;
КонецФункции
```

---

## Регистры 1С, используемые в запросах

| Регистр | Назначение |
|---|---|
| `РегистрНакопления.ЗапасыНаСкладах.Остатки` | Остатки товаров на складах |
| `РегистрНакопления.Продажи` | Продажи (обороты) |
| `РегистрНакопления.РасчетыСПокупателями` | Взаиморасчеты с клиентами |
| `РегистрНакопления.Закупки` | Закупки (обороты) |
| `РегистрСведений.ЦеныНоменклатуры` | История цен |
| `Справочник.Номенклатура` | Справочник номенклатуры |
| `Документ.ЗаказПоставщику.Запасы` | Заказы поставщикам (ТЧ Запасы) |

---

## Оптимизированные запросы (агрегация на стороне 1С)

**Файл:** `sales_aggregated.bsl`

Функции для batch-эндпоинта, возвращающие ТОЛЬКО агрегированные данные:

| Функция | Что возвращает |
|---|---|
| `ПродажиСводка(Параметры)` | `{total_sales, total_orders, avg_order, dates}` |
| `ПродажиПоТоварам(Параметры)` | Топ-N товаров `[{product, quantity, amount, orders_count}]` |
| `КлиентыТоп(Параметры)` | Топ-N клиентов `[{customer, total_sales, orders_count}]` |
| `ОстаткиТоваров(Параметры)` | Остатки с количеством > 0 `[{product, warehouse, quantity, amount}]` |
| `Неликвиды(Параметры)` | Товары без продаж за период `[{product, stock_quantity, stock_value, days_since_last_sale}]` |

## Материализованная витрина продаж по дням

**Файл:** `materialized_view_sales_daily.bsl`

**Регистр сведений:** `ВитринаПродажПоДням`
- Измерения: `Период` (день), `Номенклатура`
- Ресурсы: `Количество`, `Сумма`, `Себестоимость`, `Прибыль`

Обновление: фоновое задание каждые 15 минут + подписка на проведение документа продажи.

Запрос к витрине выполняется за < 0.1 сек (против 30 сек при прямом запросе к оборотному регистру).
